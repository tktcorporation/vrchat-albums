# セキュリティ設定

# 依存パッケージのライフサイクルスクリプト実行を制御する。
# pnpm の onlyBuiltDependencies (package.json) で許可リスト方式により管理。
# ignore-scripts=true は onlyBuiltDependencies と競合するため使用しない。

# 最高解像度モードを使用して、既知の脆弱性を含む古いバージョンの解決を防ぐ。
resolution-mode=highest
